
import 'package:flutter/material.dart';

class PrayerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Waktu Solat')),
      body: Center(child: Text('Waktu Solat & Qibla placeholder (e-Solat integration)')),
    );
  }
}
