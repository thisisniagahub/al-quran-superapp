
import 'package:flutter/material.dart';

class ReaderScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Al-Quran Reader')),
      body: Center(child: Text('Mushaf Uthmani view placeholder')),
    );
  }
}
