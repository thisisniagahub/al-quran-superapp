
import 'package:flutter/material.dart';

class StreamingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('PeaceTV-style Streaming')),
      body: Center(child: Text('Live & VOD streaming placeholder')),
    );
  }
}
